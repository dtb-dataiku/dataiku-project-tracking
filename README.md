# dataiku-project-tracking
Admin-level Dataiku DSS library for tracking projects across an instance — inventories datasets, dashboards, webapps, API services, models, and agents into normalized datasets.
